<%= form_for(@trial) do |f| %>
  <% if @trial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trial.errors.count, "error") %> prohibited this trial from being saved:</h2>

      <ul>
      <% @trial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :claim %><br>
    <%= f.text_area :claim %>
  </div>
  <div class="field">
    <%= f.label :prosecutor_id %><br>
    <%= f.number_field :prosecutor_id %>
  </div>
  <div class="field">
    <%= f.label :defender_id %><br>
    <%= f.number_field :defender_id %>
  </div>
  <div class="field">
    <%= f.label :ended_at %><br>
    <%= f.date_select :ended_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
